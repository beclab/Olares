{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "dashboard.applications.pods",
  "description": "Per-pod table for a single namespace — name / node / status / cpu / memory / restarts / age.",
  "type": "object",
  "required": ["kind", "meta", "items"],
  "properties": {
    "kind": {"const": "dashboard.applications.pods"},
    "meta": {"type": "object"},
    "items": {"type": "array", "items": {"type": "object"}}
  }
}
