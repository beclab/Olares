package settings

// This file is intentionally thin: shared printers / helpers used by the
// settings umbrella belong here as they emerge across multiple areas.
// Today the umbrella has no cross-area helpers; the file is kept so the
// package layout matches market/ on disk and future additions stay
// focused on per-verb diffs instead of churning the package skeleton.
