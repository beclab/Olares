---
outline: [2, 3]
description: Find what to set up after updating from Olares 1.12.6 to Olares 1.12.7, including Router, Lares, a local model, agent app updates, and the new model connection method for apps.
head:
  - - meta
    - name: keywords
      content: Olares 1.12.7, update notes, Router, Lares, local AI
---

# What to do after updating to Olares 1.12.7

Olares 1.12.7 changes how apps connect to models. The update does not affect your existing apps or data.

After updating, complete the following setup to use the new AI features.

:::warning Update in steps
If you are running Olares 1.12.5 or earlier, update to 1.12.6 first, then update to 1.12.7. See [What to do after updating to Olares 1.12.6](./1.12.6.md) for the migrations and configuration changes that may apply to your system.
:::

## Install Router, Lares, and a model

Starting with 1.12.7, apps on Olares reach models through Router, the AI gateway for models and tools. Lares, the official AI assistant for Olares, automatically picks up the models and tools configured in Router, with no manual configuration. Like other agent apps on Olares, Lares comes with Olares CLI and Agent Skills bundled.

To get started:

1. Open **Market** and install **Router**.
2. Install **Lares**.
3. Install a local model. For most devices, **Qwen3.8-27B** is a good starting point.

   :::info For Olares One owners: run one request at a time
   Qwen3.8-27B is a dense model, and agent tasks rely on a long context window. On Olares One, which has 24 GB of VRAM, run one request at a time to guarantee the 100K context window and precision. Wait for the current task to finish before starting the next one, as concurrent requests can slow down or get stuck.
   :::

4. Open Lares and try a simple task, such as checking this device's configuration, to confirm everything works.
<!-- TODO: Uncomment when the docs are live, and fix the paths
- [Router](/manual/TODO-router): Manage models, tools, and API keys for every app in one place.
- [Lares](/manual/TODO-lares): Give the agent a goal and let it work across your apps, files, and system.
-->

## Update agent apps

After updating the system, update the agent apps installed on Olares, such as Claude Code, from Market so they work with the new version. This also updates the Olares CLI and Agent Skills bundled with the apps.

## Connect apps through Router

Tutorials for AI and third-party apps have been updated for the Router connection flow. In short, point the app at Router's Base URL and use `default-chat` as the model name, instead of connecting to a model's own endpoint. You can change the default model in Router without reconfiguring the app.

If you configured an app before 1.12.7, update its model connection settings the next time you use it.

## Learn more

- [What's new in docs](/manual/release-notes): Find new, updated, and retired documentation for this release.

<!-- TODO: Uncomment at release
- [Olares 1.12.7 release notes](https://github.com/beclab/Olares/releases/tag/1.12.7): Review the software changes included in this release.
- [Olares 1.12.7 blog post](https://www.olares.com/blog/olares-1-12-7/): Explore the release highlights and feature walkthroughs.
-->
